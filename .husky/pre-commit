#!/usr/bin/env sh
# Pre-commit hook DISABLED per user request.
# Original logic removed temporarily. This script is a NO-OP.
# To re-enable previous safeguards, restore from git history (commit before disabling).
exit 0
