# Environment configuration for no-database mode
# Copy this to .env.no-db and modify as needed

# Server configuration
PORT=8080
GIN_MODE=debug

# Optional: Override host binding (default is all interfaces)
# HOST=localhost

# For contract validation
OPENAPI_SPEC_PATH=./docs/openapi.yaml

# Mock data settings
MOCK_MODE=true
MOCK_USER_ID=test-user-123
MOCK_USERNAME=test-user
