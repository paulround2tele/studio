delete:
  tags: [proxies]
  security:
    - cookieAuth: []
  summary: Bulk delete proxies
  operationId: proxies_bulk_delete
  requestBody:
    required: true
    content:
      application/json:
  schema: { $ref: '../../components/schemas/all.yaml#/BulkDeleteProxiesRequest' }
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            allOf:
              - $ref: '../../components/schemas/all.yaml#/SuccessEnvelope'
              - type: object
                properties:
                  data: { $ref: '../../components/schemas/all.yaml#/BulkProxyOperationResponse' }
    '400': { $ref: '../../components/responses.yaml#/BadRequest' }
    '500': { $ref: '../../components/responses.yaml#/InternalServerError' }
