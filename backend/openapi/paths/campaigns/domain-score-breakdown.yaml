get:
  tags: [campaigns]
  summary: Get detailed score breakdown for a specific domain in a campaign
  operationId: campaigns_domain_score_breakdown
  parameters:
    - name: campaignId
      in: path
      required: true
      schema: { type: string, format: uuid }
    - name: domain
      in: path
      required: true
      schema: { type: string }
  responses:
    '200':
      description: Domain score breakdown
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/all.yaml#/DomainScoreBreakdownResponse'
    '400': { $ref: '../../components/responses.yaml#/BadRequest' }
    '401': { $ref: '../../components/responses.yaml#/Unauthorized' }
    '404': { $ref: '../../components/responses.yaml#/NotFound' }
    '500': { $ref: '../../components/responses.yaml#/InternalServerError' }
