post:
  tags: [campaigns]
  summary: Get current global pattern offset for domain generation config
  operationId: campaigns_domain_generation_pattern_offset
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/all.yaml#/PatternOffsetRequest'
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/all.yaml#/PatternOffsetResponse'
  '400': { $ref: '../../components/responses.yaml#/BadRequest' }
  '401': { $ref: '../../components/responses.yaml#/Unauthorized' }
  '404': { $ref: '../../components/responses.yaml#/NotFound' }
  '500': { $ref: '../../components/responses.yaml#/InternalServerError' }